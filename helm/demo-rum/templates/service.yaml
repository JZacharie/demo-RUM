apiVersion: v1
kind: Service
metadata:
  name: {{ include "demo-rum.fullname" . }}
  labels:
    {{- include "demo-rum.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "demo-rum.selectorLabels" . | nindent 4 }}
